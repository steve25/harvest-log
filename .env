# Spoločné premenné
DB_PASSWORD=securepassword
MYSQL_ROOT_PASSWORD=root

# Dev prostredie
# APP_ENV=development
# APP_DEBUG=true
# NODE_ENV=development
# BACKEND_DOCKERFILE=Dockerfile.dev
# FRONTEND_DOCKERFILE=Dockerfile.dev
# NGINX_CONF=./nginx/nginx.dev.conf

# Prod prostredie (zakomentované, odkomentujte pre prod)
APP_ENV=production
APP_DEBUG=false
NODE_ENV=production
BACKEND_DOCKERFILE=Dockerfile.prod
FRONTEND_DOCKERFILE=Dockerfile.prod
NGINX_CONF=./nginx/nginx.prod.conf
